branches:
  only:
    - master

language: python

matrix:
  include:
    - python: 3.7
      env: TOXENV=check
    - python: 2.7
      env: TOXENV=py27
    - python: 3.5
      env: TOXENV=py35
    - python: 3.6
      env: TOXENV=py36
    - python: 3.7
      env: TOXENV=py37

install:
  - pip list
  - pip install tox codecov

script:
  - tox

before_cache:
  - rm -rf $HOME/.cache/pip/log

cache:
  directories:
    - $HOME/.cache/pip
    - $HOME/.cache/pre-commit

after_success:
  - codecov
